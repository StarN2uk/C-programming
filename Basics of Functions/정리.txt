함수
반판타입 함수이름(인자, 매계변수)
{
    함수내용
}
인자가 필요할 수도 필요없을 수도 있다. 없을 때는 void, 있을 때는 int
함수의 위치는 C언어의 순차적 절차적 특성 때문에
1. main 함수 위에 쓴다
        or
2. 프로토타이핑 -> int ReverserseNum(int n);
                  int main()
                  {
                      ...
                  }
