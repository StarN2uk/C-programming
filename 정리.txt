String
아스키코드-> 대문자를 소문자로 + 32
            소문자를 대문자로 - 32
            문자형 숫자를 정수형 숫자로 - 48
            정수형 숫자를 문자형 숫자로 + 48
            \0 (NULL) = 0
            ' '(space) = 32
            A = 65
            a = 97
getchar() = 키보드 입력으로 문자를 받아오는 함수
putchar() = 문자를 콘솔에 출력하는 함수
NULL 문자가 없다 -> 문자 배열
NULL 문자가 있다 -> 문자 배열, 문자열
1. 마지막 \0 NULL이 들어감
2. 문자열의 길이 + 1 해줘야 함
문자를 표현하는 형식문자는 %c 이고 문자열을 표현하는 형식문자는 %s입니다.
puts = 문장 출력 함수
gets = scanf 와 비슷함
문자열의 길이 구하는 방법
while(str[i] != 0)
{
    i++;
}
i = 문자열의 길이
