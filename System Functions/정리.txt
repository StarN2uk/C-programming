System Functions

정렬이란? 체계적으로 정리하는 과정 -> 처리를 쉽게하기 위해서
STR(ing)C(o)MP(are) = a와 b 문자열을 비교하여 사전 순으로 어떤 문자열이 먼저 나오는 지 반환함.
a와 b가 같다  a가 사전 순으로 빠름  b가 사전 순으로 빠름
     0                -1                  1
STR(ing) LEN(gth) = '\0'을 제외한 문자열의 길이 반환함.
STR(ing) (con)CAT(enation) = 문자열 a의 뒤에 문자열 b를 접합하여 붙여줌.
뒤에는 문자열이지만 앞에는 반드시 변수를 넣어야 한다.
예) -> char str[20] = "장";
       strcat(str, "주원");
힙에는 동적? 할당이라는 것이 들어간다.
동적이란? 너무 어려워~ 알 필요 없음;
